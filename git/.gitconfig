[core]
    editor = vim
[alias]
    br = branch
    cb = checkout -b
    co = checkout
    ci = commit
    ca = commit --amend
    d = diff
    pu = push -u origin HEAD
    st = status -sb
    undo = reset --soft HEAD^
